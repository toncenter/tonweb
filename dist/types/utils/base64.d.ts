export declare function bytesToBase64(bytes: Uint8Array): string;
export declare function base64ToBytes(base64: string): Uint8Array;
export declare function isValidBase64(base64string: string): boolean;
/**
 * @deprecated this function is no longer used by the library
 *             and will be removed in the future
 */
export declare function base64toString(base64: string): string;
/**
 * @deprecated this function is no longer used by the library
 *             and will be removed in the future
 */
export declare function stringToBase64(text: string): string;
